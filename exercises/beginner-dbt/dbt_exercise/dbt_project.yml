name: 'bookstore_exercise'
version: '1.0.0'
config-version: 2

profile: 'bookstore_exercise'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bookstore_exercise:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
